:root {
  --color-green: #4DC274;
  --color-text: rgba(18, 20, 23, 0.50);
  --color-text-title: #121417;
  --color-secondary-background: #F3F5F6;
  --color-white: #ffffff;
  --color-text-transparent: rgba(255, 255, 255, 0.50);
  --color-pseudo-element: #BEE8CC;
}

*,
*::before,
*::after {
  box-sizing: border-box;
}

body {
  font-family: 'Manrope', sans-serif;
  color: var(--color-text);
  background-color: var(--color-white);
  font-size: 14px;
  font-weight: 400;
  line-height: 1.71;
  font-style: normal;
}

.list {
  list-style: none;
}

.link {
  text-decoration: none;
}

h1,
h2,
h3,
h4,
h5,
h6,
p,
ul {
  margin: 0;
  padding: 0;
}

input,
textarea,
button {
  font: inherit;
}

button {
  cursor: pointer;
  color: currentColor;
}

ul {
  list-style: none;
}

a {
  text-decoration: none;
  outline: none;
}

img {
  display: block;
  max-width: 100%;
  height: auto;
}

.container {
  min-width: 320px;
  padding-left: 16px;
  padding-right: 16px;
  margin: 0 auto;
}






